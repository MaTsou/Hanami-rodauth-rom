%turbo-frame{ id: 'new_post' }
  = form_for :post, routes.path( :create_post ) do |f|
    = f.hidden_field :account_id, value: account_id
    = render "form", f: f, errors: errors
    %p= f.submit "Create", class: 'btn btn-primary'
